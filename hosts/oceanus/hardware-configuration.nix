# =============================================================================
# hardware-configuration.nix — PLACEHOLDER
# =============================================================================
#
# Este arquivo NÃO deve ser copiado de outro repositório.
# Ele é específico para o hardware desta máquina.
#
# Gere-o com:
#
#   sudo nixos-generate-config --show-hardware-config > hardware-configuration.nix
#
# ou após instalação:
#
#   sudo nixos-generate-config
#   # então copie /etc/nixos/hardware-configuration.nix para cá
#
# =============================================================================

{ ... }:

{
  # Placeholder — substituir pelo output de nixos-generate-config
}
